interface:
  display_name: "Batch-Complete Review"
  short_description: "Review full finding batches at proportional rigor"
  default_prompt: "Use $batch-complete-independent-review to review this change, choosing proportional rigor from the requested decision and keeping findings inside the authorized claim."

policy:
  allow_implicit_invocation: true
