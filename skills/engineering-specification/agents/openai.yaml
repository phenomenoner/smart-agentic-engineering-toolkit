interface:
  display_name: "Engineering Specification"
  short_description: "Specify behavior and acceptance before implementation"
  default_prompt: "Use $engineering-specification to frame unresolved behavior, classify material findings against the authorized deliverable and claim, conditionally test whether a proposed mechanism is necessary or a simpler direct/platform route works, then freeze falsifiable acceptance before implementation."
policy:
  allow_implicit_invocation: true
