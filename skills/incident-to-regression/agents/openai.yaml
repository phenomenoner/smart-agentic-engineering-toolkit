interface:
  display_name: "Incident to Regression"
  short_description: "Convert incidents into replayable regression gates"
  default_prompt: "Use $incident-to-regression to build a redacted discriminating regression package; prefer observed fail-first evidence when safe and available, otherwise bind a documented current-state discriminator."
policy:
  allow_implicit_invocation: true
