interface:
  display_name: "Completeness and Test Synthesis"
  short_description: "Judge readiness claims with falsifying evidence"
  default_prompt: "Use $completeness-and-test-synthesis to assess this exact readiness claim within its claim budget and, only for a costly multi-link seam, require isolated link evidence before composition and higher-altitude proof."
policy:
  allow_implicit_invocation: true
