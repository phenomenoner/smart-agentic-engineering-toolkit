interface:
  display_name: "Completeness and Test Synthesis"
  short_description: "Check readiness and complete user interactions"
  default_prompt: "Use $completeness-and-test-synthesis to assess this exact readiness claim within its claim budget, distinguish natural user journeys and affected next interactions from component passes, disposition evidence gaps at the affected effect boundary by consequence × estimated frequency, and only for a costly multi-link seam require isolated link evidence before composition and higher-altitude proof."
policy:
  allow_implicit_invocation: true
