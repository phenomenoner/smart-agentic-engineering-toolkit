interface:
  display_name: "Specify Temporal Ownership"
  short_description: "Design race-safe ownership and lifecycle contracts"
  default_prompt: "Use $specify-temporal-ownership to design a race-safe lifecycle contract and falsifiable concurrency tests for this change."

policy:
  allow_implicit_invocation: true
