interface:
  display_name: "Engineering Implementation"
  short_description: "Implement bounded changes with sufficient evidence"
  default_prompt: "Use $engineering-implementation to make this authorized change as a small coherent slice, refuse unapproved scope expansion, and for a costly multi-link seam prove independent links before composition and higher-altitude checks."

policy:
  allow_implicit_invocation: true
